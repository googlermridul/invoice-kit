// GENERATED-LIKE placeholder for the GetIt + injectable DI configuration.
//
// This file mirrors what `injectable_generator` produces when using
// `@injectable` annotations. Since the boilerplate prefers an explicit
// `configureDependencies()` for readability, this stub simply re-exports
// the symbols. Replace with `include: injection.config.dart` once you
// adopt `@injectable` codegen across the project.

export 'injection.dart';
